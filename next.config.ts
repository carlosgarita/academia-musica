import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // PWA is handled natively by Next.js 15 via app/manifest.ts
  // No additional configuration needed for Vercel deployment
};

export default nextConfig;
